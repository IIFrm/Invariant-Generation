var	x:int, y:int, k:int, i:int, j:int;

begin
	j=0;
	if (x+y==k) then
		while (brandom) do
			if(j==i) then
				x=x+1;y=y-1;
			else 
				y=y+1;x=x-1;
			endif;
			j=j+1; 
		done;
	//x+y==k

	endif;
end
