var	x:int, y:int;

begin
	if x*y>=0 then 

	while nondet() do
		if x==0 then
			{ 
				if y>0 then
				  x++; 
				else 
				  x--;
				 endif;
			} 
		if x>0 then
			y++; 
		else 
			x--;
		endif;
	done;	//x*y>=0

	endif;
end
