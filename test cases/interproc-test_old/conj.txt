var	x:int;

begin
	int loop_times;
	loop_times = 10;
	if (x>=0) && (x<=50) then 

	while loop_times-->0 do
		 if (x>50) then
		 x++;
		 endif;
		if (x == 0) then {
			x ++;
		} else {
			x--;
		}
	endif;
	done;	//(x>=0) && (x<=50)

	endif;
end
